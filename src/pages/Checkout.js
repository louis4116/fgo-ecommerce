import React, { Fragment } from "react";
import Form from "../components/form/Form";

const Checkout = () => {
  return (
    <Fragment>
      <Form />
    </Fragment>
  );
};

export default Checkout;
